{
  "name": "pi-tiny-boss",
  "version": "0.2.0",
  "description": "A local System One decision model picks your tools before the big one does. Laya scores typed need questions about your prompt and the frontier model is handed the resulting tool plan instead of the guesswork. Fully offline, zero API cost.",
  "type": "module",
  "main": "./index.ts",
  "keywords": [
    "pi",
    "pi-package",
    "pi-extension",
    "laya",
    "system-one",
    "decision-model",
    "onnx"
  ],
  "author": "mastnacek",
  "license": "MIT",
  "files": [
    "README.md",
    "package.json",
    "./index.ts",
    "src",
    "eval",
    "test"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "npx tsx --test test/**/*.test.js",
    "eval": "npx tsx eval/accuracy.ts && npx tsx eval/gates.ts && npx tsx eval/report.ts",
    "eval:compare": "npx tsx eval/compare.ts",
    "eval:report": "npx tsx eval/report.ts"
  },
  "dependencies": {
    "@receptron/laya": "^0.1.2"
  }
}
